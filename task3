#include <iostream>
#include <vector>
#include <set>
#include <algorithm>
#include <iterator>

using namespace std;

int main() {
    vector<int> V = {9, 2, 7, 2, 5, 9, 1, 5, 3};

    set<int> distinct_sorted(V.begin(), V.end());

    auto first_rev = next(distinct_sorted.rbegin());
    auto last_rev  = prev(distinct_sorted.rend());

    copy(first_rev, last_rev,
              ostream_iterator<int>(cout, " "));

    return 0;
}